version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Build
    task:
      jobs:
        - name: docker build
          commands:
            - checkout
            - 'echo "${Nikitha@1999}" | docker login -u "${nikithakaluvagadda}" --Nikitha@1999-stdin'
            - 'docker pull "${nikithakaluvagadda}/RestApi_crud_application-addressbook:latest" || true'
            - 'docker build --cache-from "${nikithakaluvagadda}/RestApi_crud_application-addressbook:latest" -t "${nikithakaluvagadda}/RestApi_crud_application-addressbook:latest" .'
            - 'docker push "${nikithakaluvagadda}/RestApi_crud_application-addressbook:latest"'
      secrets:
        - name: dockerhub
